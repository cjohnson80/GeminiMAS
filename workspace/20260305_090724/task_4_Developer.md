{"thought": "The SENIOR_DEBUGGER_ADVICE provides a clear, three-step recovery plan to bypass previous tool instability and get back on track. I must first execute Step 1: write the required code for `lib/heartbeat_executor.py` using the `write_file` tool, ensuring the JSON payload is correctly formatted with escaped newlines.", "tool": "write_file", "payload": "{\"path\":\"lib/heartbeat_executor.py\", \"content\":\"import logging\\nimport time\\nfrom concurrent.futures import ThreadPoolExecutor\\nfrom queue import Queue, Empty\\nfrom typing import Callable, Any\\n\\nlogger = logging.getLogger(__name__)\\n\\nclass HeartbeatExecutor:\\n    def __init__(self, max_workers: int = 5):\\n        self.max_workers = max_workers\\n        self.queue = Queue()\\n        self.executor = ThreadPoolExecutor(max_workers=self.max_workers)\\n        self.running = False\\n        self._future_map = {}\\n\\n    def submit_task(self, task_func: Callable, *args, **kwargs):\\n        \\\"\\\"Submit a task to the internal queue.\\\"\\\"\\n        self.queue.put((task_func, args, kwargs))\\n\\n    def start(self):\\n        \\\"\\\"Start processing the queue.\\\"\\\"\\n        self.running = True\\n        logger.info(f\\\"HeartbeatExecutor started with {self.max_workers} workers.\\\")\\n        # We start a background daemon or rely on direct submission depending on architecture. \\n        # However, typical executor pattern suggests simply submitting to ThreadPool:\\n        self.executor.submit(self._process_queue)\\n\\n    def _process_queue(self):\\n        while self.running:\\n            try:\\n                # Block briefly to allow shutdown check\\n                task_func, args, kwargs = self.queue.get(timeout=1.0)\\n                future = self.executor.submit(task_func, *args, **kwargs)\\n                # Optional: Store future if tracking is needed\\n            except Empty:\\n                continue\\n            except Exception as e:\\n                logger.error(f\\\"Error processing queue: {e}\\\")\\n\\n    def stop(self):\\n        \\\"\\\"Graceful shutdown.\\\"\\\"\\n        self.running = False\\n        self.executor.shutdown(wait=True)\\n        logger.info(\\\"HeartbeatExecutor stopped.\\\")\"}"}