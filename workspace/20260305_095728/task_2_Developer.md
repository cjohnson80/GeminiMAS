{"thought": "The SENIOR_DEBUGGER_ADVICE correctly diagnoses a JSON escaping issue when using `write_file` for multi-line code blocks. I will follow the primary recommendation to use the `run_shell` tool with a heredoc (`cat << 'EOF' > ...`) to reliably create the `bin/heartbeat_utils.py` file, bypassing the JSON string parser.", "tool": "run_shell", "payload": "cat << 'EOF' > bin/heartbeat_utils.py\nimport re\nimport os\n\nHEARTBEAT_FILE = \"/home/chrisj/gemini_agents/core/HEARTBEAT.md\"\n\ndef parse_tasks(content):\n    \"\"\"Extracts incomplete tasks (lines starting with - [ ]) from the content.\"\"\"\n    # Regex to find incomplete tasks: starts with optional whitespace, then '- [ ', followed by content until the end of the line.\n    incomplete_tasks = re.findall(r'^\\s*-\\s*\\[\\s*\\]\\s*(.*)$', content, re.MULTILINE)\n    return incomplete_tasks\n\ndef update_task_status(content, task_description_fragment, mark_complete=True):\n    \"\"\"Updates the first matching task status in the content string.\"\"\"\n    if not content:\n        return content\n\n    # Regex to find the specific task line and capture its prefix and description\n    # This looks for '- [ ] <description>' or '- [x] <description>'\n    pattern = re.compile(r'^(.*)(\\s*-\\s*\\[\\s*([ x])\\s*\\]\\s*)(' + re.escape(task_description_fragment) + r'.*)$', re.MULTILINE)\n    \n    new_status = '[x]' if mark_complete else '[ ]'\n    \n    def replacer(match):\n        prefix = match.group(1)\n        task_line_prefix = match.group(2).strip() # e.g., '- [ ]'\n        current_status_char = match.group(3)\n        description = match.group(4)\n        \n        if current_status_char == 'x' and mark_complete:\n            # Already complete, return original line\n            return match.group(0)\n        if current_status_char == ' ' and not mark_complete:\n            # Already incomplete, return original line\n            return match.group(0)\n            \n        # Construct the new line\n        return f\"{prefix} - [{new_status[1]}] {description}\"\n\n    # Use re.sub with the replacer function\n    updated_content = pattern.sub(replacer, content)\n    \n    return updated_content\n\n\nif __name__ == \"__main__\":\n    # --- Demonstration ---\n    print(\"--- Reading HEARTBEAT.md ---\")\n    try:\n        with open(HEARTBEAT_FILE, 'r') as f:\n            initial_content = f.read()\n            print(initial_content)\n            \n            print(\"\\n--- Extracted Incomplete Tasks ---\")\n            tasks = parse_tasks(initial_content)\n            for i, task in enumerate(tasks):\n                print(f\"{i+1}. {task.strip()}\")\n\n            if tasks:\n                # Example: Mark the first identified incomplete task as done\n                first_task_desc = tasks[0].strip()\n                print(f\"\\n--- Attempting to update task: '{first_task_desc}' to [x] ---\")\n                \n                # We need a robust way to find the line to update. \n                # For this script demonstration, we'll just show the logic works on the string.\n                # A full implementation would require reading, modifying, and writing back.\n                \n                # Simple check to see if update_task_status finds the line fragment\n                # Note: update_task_status is complex due to context dependency, \n                # for this initial utility, we focus on parsing/extraction.\n                \n                # Let's try to update the line containing the fragment 'Implement initial parsing logic'\n                test_fragment = \"Implement initial parsing logic\"\n                if test_fragment in initial_content:\n                    print(f\"Found fragment '{test_fragment}'. Logic defined for updating.\")\n                else:\n                    print(f\"Fragment '{test_fragment}' not found in current content for testing update logic.\")\n\n    except FileNotFoundError:\n        print(f\"Error: HEARTBEAT_FILE not found at {HEARTBEAT_FILE}\")\n\nEOF"}