{
  "tool": "write_file",
  "payload": "{\"path\": \"/home/chrisj/gemini_agents/bin/tg_gateway.py\", \"content\": \"import json\\nimport os\\nimport subprocess\\nimport logging\\n\\nPENDING_APPROVAL_FILE = \\\"pending_approval.json\\\"\\n\\ndef save_pending_approval(branch=\\\"origin/main\\\"):\\n    with open(PENDING_APPROVAL_FILE, \\\"w\\\") as f:\\n        json.dump({\\\"status\\\": \\\"pending\\\", \\\"branch\\\": branch}, f)\\n\\ndef get_pending_approval():\\n    if os.path.exists(PENDING_APPROVAL_FILE):\\n        with open(PENDING_APPROVAL_FILE, \\\"r\\\") as f:\\n            return json.load(f)\\n    return None\\n\\ndef clear_pending_approval():\\n    if os.path.exists(PENDING_APPROVAL_FILE):\\n        os.remove(PENDING_APPROVAL_FILE)\\n\\ndef handle_approve(chat_id, send_message_func):\\n    subprocess.run([\\\"git\\\", \\\"fetch\\\"], check=True)\\n    try:\\n        diff_output = subprocess.check_output([\\\"git\\\", \\\"diff\\\", \\\"--stat\\\", \\\"HEAD..origin/main\\\"], stderr=subprocess.STDOUT, text=True)\\n    except subprocess.CalledProcessError as e:\\n        diff_output = f\\\"Error generating diff: {e.output}\\\"\\n\\n    if not diff_output.strip():\\n        send_message_func(chat_id, \\\"No new changes to approve.\\\")\\n        return\\n\\n    save_pending_approval(\\\"origin/main\\\")\\n    msg = f\\\"⚠️ **Pending Approval** ⚠️\\\\n\\\\nDiff Summary:\\\\n```\\\\n{diff_output}\\\\n```\\\\n\\\\nReply with **approve** to merge and install, or **reject** to abort.\\\"\\n    send_message_func(chat_id, msg)\\n\\ndef process_incoming_message(chat_id, text, send_message_func):\\n    text_lower = text.strip().lower()\\n    pending_state = get_pending_approval()\\n\\n    if pending_state and pending_state.get(\\\"status\\\") == \\\"pending\\\":\\n        if text_lower == \\\"approve\\\":\\n            send_message_func(chat_id, \\\"Approval received. Merging and running install.sh...\\\")\\n            subprocess.run([\\\"git\\\", \\\"merge\\\", pending_state[\\\"branch\\\"]], check=True)\\n            subprocess.run([\\\"./install.sh\\\"], check=True)\\n            clear_pending_approval()\\n            send_message_func(chat_id, \\\"✅ Update complete.\\\")\\n            return\\n        elif text_lower == \\\"reject\\\":\\n            clear_pending_approval()\\n            send_message_func(chat_id, \\\"❌ Update rejected. State cleared.\\\")\\n            return\\n        else:\\n            send_message_func(chat_id, \\\"You have a pending update. Please reply with 'approve' or 'reject'.\\\")\\n            return\\n\\n    if text_lower == \\\"/approve\\\":\\n        handle_approve(chat_id, send_message_func)\\n    # ... existing routing logic ...\"}"
}