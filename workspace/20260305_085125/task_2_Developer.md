{"thought": "The previous attempts failed due to JSON escaping errors when writing the file content. I must adhere strictly to the SENIOR_DEBUGGER_ADVICE and use the provided, correctly formatted JSON payload to create `lib/heartbeat_parser.py` using the standard library's regex capabilities, as searching for external libraries is no longer necessary.", "tool": "write_file", "payload": "{\"path\": \"lib/heartbeat_parser.py\", \"content\": \"import re\\n\\ndef parse_heartbeat(file_path):\\n    \\\"\\\"\\\"\\n    Parses the HEARTBEAT.md file to extract the next task.\\n    \\\"\\\"\\\"\\n    try:\\n        with open(file_path, 'r') as f:\\n            content = f.read()\\n            \\n        # Simple regex to find the first unchecked task\\n        # Looks for '- [ ] Task Name' or '[ ] Task Name' or 'TODO: Task Name'\\n        # Updated regex to be more robust as per task requirement.\\n        # Pattern: Matches '[ ]', 'TODO', or 'Pending' followed by the task description.\\n        pattern = re.compile(r'(?:-\s*\\[ \\]|TODO:|Pending:)\\s*(.*)', re.IGNORECASE)\\n        \\n        match = pattern.search(content)\\n        \\n        if match:\\n            return match.group(1).strip()\\n        return None\\n    except FileNotFoundError:\\n        print(f'Error: Heartbeat file not found at {file_path}')\\n        return 'File Missing'\\n    except Exception as e:\\n        print(f'Error reading heartbeat: {e}')\\n        return None\"}"}