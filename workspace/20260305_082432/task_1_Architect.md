{"thought": "The previous failures confirm that the `write_file` tool requires the content to be a properly escaped JSON string. I need to define the core components: `HeartbeatInstruction` model, `HeartbeatParser`, `TaskExecutor`, and `StateManager`. Since the previous attempt failed due to improper content formatting, I will now define the structure for `src/heartbeat/models.py` and ensure the content string is correctly formatted for the JSON payload, escaping all internal quotes and newlines.", "tool": "write_file", "payload": "{\"path\": \"src/heartbeat/models.py\", \"content\": \"from dataclasses import dataclass, field\\nfrom typing import List, Dict, Any\\n\\n@dataclass\\nclass HeartbeatInstruction:\\n    id: str\\n    action: str  # e.g., 'EXECUTE_TASK', 'UPDATE_STATE'\\n    target: str  # e.g., 'shell', 'config'\\n    payload: Dict[str, Any] = field(default_factory=dict)\\n    required_permissions: List[str] = field(default_factory=list)\\n\\n    def __str__(self):\n        return f\\\"[Instruction {self.id}] Action: {self.action}, Target: {self.target}\\\"\n\\n# Placeholder for State Management Lock (to be implemented in StateManager)\\n# class StateLock: ...\"}"}