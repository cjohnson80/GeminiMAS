{"thought": "The previous search attempts failed due to tool output structure issues (HTML instead of clean data), as pointed out by the SENIOR_DEBUGGER_ADVICE. I must bypass further searching and directly create the required documentation file, `/home/chrisj/gemini_agents/knowledge/nextjs_ts_standards_2024.md`, using the provided best-practice content.", "tool": "write_file", "payload": "{\"path\": \"/home/chrisj/gemini_agents/knowledge/nextjs_ts_standards_2024.md\", \"content\": \"# Next.js App Router & TypeScript Standards (2024 Edition)\\n\\n## 1. Architectural Principles\\n*   **Server Components by Default:** All components are React Server Components (RSC) unless explicitly marked with `'use client'`.\\n    *   *Benefit:* Reduced bundle size, direct backend access.\\n*   **Colocation:** Keep related files (components, styles, tests) grouped by feature/route rather than technical role (e.g., avoid a global `/components` folder for feature-specific UI).\\n\\n## 2. Data Fetching & Mutations\\n*   **Fetch in Server Components:** Retrieve data directly in RSCs using `await` (async components). Avoid `useEffect` for initial data loading.\\n*   **Server Actions:** Use Server Actions (`'use server'`) for all mutations (POST/PUT/DELETE) instead of Route Handlers (API routes), unless an external webhook is required.\\n    *   Actions must validate inputs on the server side.\\n*   **TanStack Query (React Query):** Optional in App Router, but recommended for complex client-side interactions (polling, optimistic updates) where Server Actions are insufficient.\\n\\n## 3. Type Safety & Validation\\n*   **Zod for Validation:** All external inputs (Forms, URL Params, API responses) must be validated with Zod schemas.\\n    *   *Pattern:* `schema.parse(data)` or `schema.safeParse(data)` inside Server Actions.\\n*   **TypeScript Strictness:** `strict: true` in `tsconfig.json`. No `any`.\\n*   **Typed Props:** All components must define interfaces for props.\\n\\n## 4. State Management\\n*   **URL as Source of Truth:** Prefer URL Search Params (`useSearchParams`) over `useState` for filter/pagination state to ensure shareability.\\n*   **Zustand:** Use for global client-side state (e.g., sidebar toggles, user session cache) if Context API becomes too complex.\\n\\n## 5. File Structure Convention\\n```text\\napp/\\n├── (marketing)/      # Route groups for layout isolation\\n├── dashboard/\\n│   ├── _components/  # Private components specific to this route\\n│   ├── actions.ts    # Server Actions for this feature\\n│   ├── page.tsx\\n│   └── layout.tsx\\n├── global-error.tsx  # Global error boundary\\n└── layout.tsx        # Root layout\\n```\\n\\n## 6. Error Handling\\n*   Use `error.tsx` files for granular error boundaries.\\n*   Use `notFound()` from `next/navigation` for 404s.\\n\"}"}